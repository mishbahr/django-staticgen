============
Installation
============

At the command line::

    $ easy_install django-staticgen

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-staticgen
    $ pip install django-staticgen
